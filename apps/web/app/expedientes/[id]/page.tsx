import Link from "next/link";
import { Expedient, formatDate, getApi } from "../../lib";
export default async function Detail({params}:{params:Promise<{id:string}>}) { const {id}=await params; const response=await getApi<{data:Expedient}>(`/expedients/${id}`); const item=response?.data; if(!item) return <main><h1 className="text-3xl font-bold">Expediente no disponible</h1><p>Puede que aún no esté sincronizado.</p></main>; return <main className="max-w-4xl space-y-6"><Link href="/expedientes" className="font-semibold text-paraguay">← Volver al listado</Link><header className="card"><p className="font-mono text-paraguay">{item.number}</p><h1 className="mt-3 text-3xl font-black">{item.title}</h1><p className="mt-4 text-slate-600">{item.chamber} · {item.type} · {item.status}</p></header><section className="grid gap-4 sm:grid-cols-3"><div className="card"><b>Etapa</b><p>{item.stage ?? "Sin datos"}</p></div><div className="card"><b>Ingreso</b><p>{formatDate(item.filed_on)}</p></div><div className="card"><b>Frescura</b><p>{formatDate(item.synced_at)}</p></div></section><section className="card"><h2 className="text-xl font-bold">Autores</h2><ul className="mt-3 list-disc pl-5">{item.authors?.map(a=><li key={a.name}>{a.name}{a.party ? ` · ${a.party}`:""}</li>) || <li>Sin datos</li>}</ul></section><section className="card"><h2 className="text-xl font-bold">Comisiones</h2><p className="mt-3">{item.committees?.join(" · ") || "Sin datos"}</p></section><section className="card"><h2 className="text-xl font-bold">Adjuntos</h2><ul className="mt-3 space-y-2">{item.attachments?.map(a=><li key={a.id}>{a.url ? <a className="text-paraguay underline" href={a.url}>{a.info ?? "Archivo"}</a> : a.info}</li>) || <li>Sin adjuntos</li>}</ul>{item.source_url && <a className="mt-5 inline-block text-sm font-semibold text-paraguay" href={item.source_url}>Ver fuente SILpy ↗</a>}</section></main>; }
