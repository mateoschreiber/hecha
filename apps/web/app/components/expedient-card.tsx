import { Expedient, formatDate } from "../lib";
export function ExpedientCard({item}: {item: Expedient}) { return <article className="card flex flex-col gap-3"><div className="flex justify-between gap-3"><span className="font-mono text-sm text-paraguay">{item.number ?? `#${item.source_id}`}</span><span className="rounded-full bg-slate-100 px-2 py-1 text-xs">{item.status ?? "Sin estado"}</span></div><h2 className="line-clamp-3 text-lg font-bold"><a href={`/expedientes/${item.source_id}`}>{item.title}</a></h2><p className="text-sm text-slate-600">{item.chamber} · {item.type}</p><footer className="text-sm text-slate-500">Ingreso: {formatDate(item.filed_on)}</footer></article>; }
